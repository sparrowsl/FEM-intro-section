{
	"name": "fem-intro-section",
	"version": "0.0.1",
	"scripts": {
		"watch": "npx tailwindcss -i ./css/app.css -o ./css/bundle.css -w -m",
		"lint": "prettier --check --plugin-search-dir=. .",
		"format": "prettier --write --plugin-search-dir=. ."
	},
	"devDependencies": {
		"prettier": "^2.7.1",
		"prettier-plugin-tailwindcss": "^0.1.12",
		"tailwindcss": "^3.1.4"
	},
	"type": "module"
}
